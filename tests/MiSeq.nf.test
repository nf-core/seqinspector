def pipelines_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/'

nextflow_pipeline {

    name "Test pipeline"
    script "../main.nf"

    def test_scenario = [
        [
            name: "MiSeq data test (single end reads)",
            params: [
                tools_bundle: 'minimal',
            ],
            rundir_folder: pipelines_testdata_base_path + 'seqinspector/testdata/MiSeq/220422_M11111_0222_000000000-K9H97.tar.gz',
            rundir_samplesheet: pipelines_testdata_base_path + 'seqinspector/testdata/MiSeq/samplesheet.csv'
        ],
        [
            name: "Miseq data test (no fastqc)",
            params: [
                input: pipelines_testdata_base_path + 'seqinspector/testdata/MiSeq/samplesheet.csv',
                skip_tools: 'fastqc',
                tools_bundle: 'minimal',
            ]
        ],
        [
            name: "Miseq data test (skip all tools)",
            params: [
                input: pipelines_testdata_base_path + 'seqinspector/testdata/MiSeq/samplesheet.csv',
                skip_tools: 'fastqc,fastqscreen,fq_lint,picard_collectmultiplemetrics,seqfu_stats',
                tools_bundle: 'minimal',
            ]
        ],
        [
            name: "MiSeq data test (paired end reads)",
            params: [
                input: pipelines_testdata_base_path + 'seqinspector/testdata/Miseq_PairedEnd/samplesheet.csv',
                tools_bundle: 'minimal',
            ]
        ]
    ]

    // Generate tests for each scenario
    test_scenario.each { scenario ->
        test(scenario.name, UTILS.getTest(scenario))
    }
}
